<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CooperativaLaVera"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Pantalla de inicio: la que se abre al arrancar la app -->
        <activity
            android:name=".activities.ActividadInicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Resto de pantallas (singleTop para no apilar duplicados al navegar) -->
        <activity android:name=".activities.ActividadLogin" android:launchMode="singleTop" />
        <activity android:name=".activities.ActividadRegistro" android:launchMode="singleTop" />
        <activity android:name=".activities.ActividadProductos" android:launchMode="singleTop" />
        <activity android:name=".activities.ActividadDetalleProducto" />
        <activity android:name=".activities.ActividadPortal" android:launchMode="singleTop" />
        <activity android:name=".activities.ActividadMisFincas" android:launchMode="singleTop" />
        <activity android:name=".activities.ActividadAdministracion" android:launchMode="singleTop" />
    </application>

</manifest>
